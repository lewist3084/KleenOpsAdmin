// Stub for FieldInfoDialog — the admin app doesn't have the full
// field-info / AI assistant infrastructure.

import 'package:flutter/material.dart';

class FieldInfoDialog {
  static Future<void> show(
    BuildContext context, {
    required String infoKey,
    String? title,
  }) async {
    // No-op in admin app.
  }
}
