// features\engagement\details\likert_survey_details.dart
//
// DEGRADED in the kleenops_admin port: depends on a screen/form that was
// stubbed in this batch (parent uses stripped infrastructure — AI canvas /
// repositories / lookup-selector / portal / AITextField / google docs).
// The stub keeps callers buildable.

import 'package:flutter/material.dart';

class _SkippedStub extends StatelessWidget {
  const _SkippedStub({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Padding(
          padding: EdgeInsets.all(24),
          child: Text(
            'This screen has not been ported to the admin app yet.',
            textAlign: TextAlign.center,
          ),
        ),
      ),
    );
  }
}
