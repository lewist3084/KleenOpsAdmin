import 'package:flutter/material.dart';

class SchedulingFloorContent extends StatelessWidget {
  const SchedulingFloorContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Floor Content', style: TextStyle(fontSize: 20)),
    );
  }
}
