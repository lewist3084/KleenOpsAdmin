import 'package:flutter/material.dart';

class ObjectsFloorsContent extends StatelessWidget {
  const ObjectsFloorsContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Objects Floors', style: TextStyle(fontSize: 20)),
    );
  }
}
