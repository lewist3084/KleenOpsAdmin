// lib/features/inventory/screens/inventory_restock.dart

import 'package:flutter/material.dart';

class InventoryRestockContent extends StatelessWidget {
  const InventoryRestockContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Restock Content', style: TextStyle(fontSize: 20)),
    );
  }
}
