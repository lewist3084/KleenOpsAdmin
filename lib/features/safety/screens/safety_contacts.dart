// lib/features/safety/screens/safety_contacts_content.dart

import 'package:flutter/material.dart';

class SafetyContactsContent extends StatelessWidget {
  const SafetyContactsContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Contacts Content', style: TextStyle(fontSize: 20)),
    );
  }
}
