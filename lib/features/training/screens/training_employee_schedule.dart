// lib/features/training/screens/training_employee_schedule.dart

import 'package:flutter/material.dart';

class TrainingEmployeeScheduleContent extends StatelessWidget {
  final String? teamId;
  const TrainingEmployeeScheduleContent({super.key, this.teamId});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Schedule coming soon.'));
  }
}
