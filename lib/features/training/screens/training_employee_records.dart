// lib/features/training/screens/training_employee_records.dart

import 'package:flutter/material.dart';

class TrainingEmployeeRecordsContent extends StatelessWidget {
  final String? teamId;
  const TrainingEmployeeRecordsContent({super.key, this.teamId});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Records coming soon.'));
  }
}
