import 'package:flutter/material.dart';

class ProcessesFloorContent extends StatelessWidget {
  const ProcessesFloorContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Processes Floor', style: TextStyle(fontSize: 20)),
    );
  }
}
